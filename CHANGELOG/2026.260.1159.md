# 2026.260.1159 — feat: author the `charly authoring` command skill

## Summary

`charly box` authoring — `set` / `add-candy` / `rm-candy` / `write` / `cat` /
`fetch` / `refresh` — had no owning skill. Adds a `skill:` entity as a SIBLING
top-level node (`authoring-skill:` beside the candy node) in
`candy/plugin-authoring/charly.yml`, family `authoring` (declared in
opencharly/pod-charly-hooks#6, MERGED), documenting the authoring verbs.

Also in this diff: the candy's `plan:` gains a deterministic build-context check
(`id: authoring-skill-present`, `command: "grep -q '^authoring-skill:' charly.yml"`)
so the plan fails when the entity is removed; and the file's missing trailing
newline is repaired.

The generated marketplace corpus is never hand-edited; this candy entity is its
source.

## How tested

**Manifest gate (pasted).** The validating binary is the repo's CI pin: `deploy.yml`
clones `charly` at `v2026.238.1242` and builds it with
`-ldflags "-X main.BuildCalVer=candy-ci"`, so `charly version` prints the ldflags
stamp (`candy-ci`), not the tag — the tag is the clone ref, the stamp is the
binary's built-in string:
```
$ charly version
candy-ci
$ charly box validate
exit 0 (the pinned charly validates the manifest cleanly; `charly box validate`
is silent on this schema — the exit code is the verdict)
```

**In-tree coverage that FAILS without the change (the plan's own discriminator):**
```
$ grep -q '^authoring-skill:' candy/plugin-authoring/charly.yml && echo PRESENT
PRESENT                                   # exit 0
$ # remove the "authoring-skill:" node
$ grep -q '^authoring-skill:' candy/plugin-authoring/charly.yml; echo exit=$?
exit=1
```

**Documented-surface provenance (R4a) — the shipped source of the clauses the
skill states:**
```
$ sed -n '20,33p' candy/plugin-authoring/authoring.go
case "set":       return dispatchSet(args)
case "add-candy": return dispatchAddCandy(args)
case "rm-candy":  return dispatchRmCandy(args)
case "write":     return dispatchWrite(args)
case "cat":       return dispatchCat(args)
case "fetch":     return dispatchFetch(hc, args)
case "refresh":   return dispatchRefresh(hc, args)

$ sed -n '51,54p' candy/plugin-authoring/authoring.go
type setGrammar struct {
    Path  string `arg:"" help:"Dot-path into charly.yml …"`
    Value string `arg:"" help:"Value (parsed as YAML; use [a,b] for lists, {x: y} for maps)"`
}
$ sed -n '115,119p' candy/plugin-authoring/authoring.go
type writeGrammar struct {
    Path    string `arg:"" help:"Path under the project root (relative; .. is rejected)"`
    Content string `name:"content" help:"File content (mutually exclusive with --from-stdin)"`
    FromIn  bool   `name:"from-stdin" help:"Read file content from stdin"`
}
$ sed -n '15,17p' candy/plugin-authoring/authoring.go
// (set/add-candy/rm-candy/write/cat) run entirely on sdk/kit + stdlib (authoring_edit.go); fetch
// and refresh reach the host-coupled repo resolver over the reverse channel (hc.fetchResolve,
// HostBuild("box-fetch-resolve")).
```

**Cross-references resolve (pasted):**
```
$ for ref in /charly-image:image /charly-image:layer /charly-build:build; do
    fam=${ref#/charly-}; fam=${fam%%:*}; name=${ref##*:}
    [ -f "marketplace/$fam/skills/$name/SKILL.md" ] && echo "OK   $ref" || echo "MISS $ref"
  done
OK   /charly-image:image
OK   /charly-image:layer
OK   /charly-build:build
```

**Family dependency is on the consumer's MERGED `main`:**
```
$ git -C pod-charly-hooks show origin/main:charly.yml | grep -n '^            authoring:'
940:            authoring:
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow, skills, marketplace, check loaded. |
| R1 — RCA every anomaly | Gate zero warnings/errors (exit 0 pasted); family read from merged main. |
| R2 — finish the whole cutover | This skill + its family (#6, MERGED) land as one set; the corpus projection is the marketplace refs-list/regeneration PR (the standard pin-bump PR once this repo tags past its merge). |
| R3 — no duplication | One `skill:` entity; documents the existing command. |
| R4 — no workarounds | Every command/flag the skill names is in the shipped source (pasted). |
| R4a — product-first docs | Documented surface is the shipped source (pasted). |
| R5 — no legacy path | Additive; a dangling sibling ref was removed. |
| R6 — git safety | Branch-per-change PR; no force-push; clean tree. |
| R7 — prove behaviour | The plan discriminator above (present→0, removed→1). |
| R8 — preserve emitted artifacts | The entity is a generated-corpus input; its projection is the regeneration gate. |
| R9 — binary equals source | Gate run with the repo's CI-pin binary (clone ref pasted; the `candy-ci` stamp is that build's ldflags). |
| R10 — fresh disposable proof | A `skill:` entity's product is its corpus projection (prose cannot fail a runtime bed); the manifest gate + plan discriminator are the in-tree proof. |
| RDD/ADE/SDD | ADE: a `skill:` entity is not a candy. SDD: entity validates against `#SkillInput`. |
| Disposable-only gate | N/A — nothing created/destroyed. |
| Hooks doctrine | N/A — merge gate = org pr-validator. |

## Change classification

- **Class:** candy manifest entity (config) — additive `skill:` entity + one
  in-tree plan check + a newline repair.
- **Verification gate:** `charly box validate` + the plan presence discriminator +
  the pasted source provenance.
- **Attribution tier:** `analysed on a live system` — the gate + discriminator ran
  live with retained output.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*